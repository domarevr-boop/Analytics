export default function CompetitorMultiChoice({ label, values, options, max, onChange }: { label: string; values: string[]; options: Array<{ value: string; label: string }>; max: number; onChange: (values: string[]) => void }) {
  return <details className="competitor-multi-choice"><summary><span>{label}</span><strong>{values.length ? `${values.length} выбрано` : 'Все'}</strong></summary><div>{options.map(option => { const checked = values.includes(option.value); const disabled = !checked && values.length >= max; return <label key={option.value}><input type="checkbox" checked={checked} disabled={disabled} onChange={() => onChange(checked ? values.filter(value => value !== option.value) : [...values, option.value])} /><span>{option.label}</span></label>; })}<small>Можно выбрать до {max}; пустой выбор означает все значения</small></div></details>;
}
