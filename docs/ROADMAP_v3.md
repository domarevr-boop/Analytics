# v3.0 Roadmap

## 1. Навигация — группировка по разделам

Текущая NavBar плоская: `Главная | План | Рентабельность | | Справочник | Импорт | Админ`

Новая структура с dropdown:

- **Главная** (dashboard)
- **Аналитика** ▾ → Воронка / Точки входа / Поисковые фразы (заглушки)
- **Бизнес-метрики** ▾ → Рентабельность (сущ.) / План (сущ.)
- **Справочник**
- **Импорт**
- **Админ**

### Требуемые изменения
- `src/types/index.ts` — расширить `PageName`, добавить подтипы
- `src/components/NavBar.tsx` — dropdown-группы, активный пункт, подсветка
- `src/App.tsx` — роутинг по новым страницам

## 2. Аналитика — страницы-заглушки

Три страницы с одинаковой структурой: фильтр дат + DashboardBlock + заглушка контента.
→ `src/pages/analytics/FunnelPage.tsx`, `EntryPointsPage.tsx`, `SearchPhrasesPage.tsx`

## 3. Баги — уточнить у пользователя

- "проблема с фильтром" — ✅ решена (две независимые панели, без авто-сброса)
- "все метрики работают корректно" — есть ли известные баги расчёта?

## ❌ Критический баг: даты импорта сдвинуты на -1 день

**Причина:** `src/data/parseFile.ts:86-94` — `fmtCell` использует `getUTC*()` вместо локальных геттеров. Для часовых поясов восточнее UTC (Москва UTC+3) Excel-дата читается на день раньше (08.07 → 07.07).

**Статус:** Не решено

**Что сделано:**

| Шаг | Что | Статус |
|---|---|---|
| 1 | `fmtCell` — `getUTC*()` → локальные геттеры | ✅ для новых импортов |
| 2 | Миграция v1: детекция `metricMax < logMax` | ❌ после переимпорта оба = July 8 |
| 3 | Миграция v1: удаление дубликатов по совпадению значений | ❌ переимпорт был до фикса, дубликатов нет |
| 4 | Миграция v1: сдвиг при охвате 2 месяцев + дата после последней метрики | ❌ `uploadedAt` в UTC, `metricMax` локально — сравнение ложно |

**Нужно:** Переделать детекцию сдвига в миграции v1 (`src/data/store.ts:763-...`). Возможное решение — сравнивать `uploadedAt` и `metricMax` в одной временной зоне, или добавить флаг при импорте о версии парсера.

## 4. UI/UX полировка (опционально)

- Состояния загрузки/пусто/ошибка на всех страницах
- Анимации переходов
- Mobile-адаптация NavBar
- Единый стиль пустых состояний

---

## Решено
- ✅ DateRangeFilter разделён на две независимые панели (Период + Сравнение)
- ✅ Фильтр больше не сбрасывается на дефолт при обновлении данных
