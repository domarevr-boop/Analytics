# Roadmap Analytics MVP

Проверено: **7 сентября 2026**. Production — **V4**, активная подготовка — **V5**.

Roadmap содержит только незавершённую работу. Принятые результаты фиксируются в version-документах; этапы, зависимости, проверки и откат V5 описаны в [V5_MIGRATION_PLAN](V5_MIGRATION_PLAN.md).

## V5 — ближайший этап: пилот «Рынок»

- [x] Владелец создал отдельный Supabase-проект V5; локальный URL/publishable key проверены и не попадают в Git.
- [x] Зафиксированы идентификаторы V4/V5 и автоматическая защита ветки, worktree, env URL, ключа и CLI link от запуска на другом проекте.
- [x] Supabase CLI авторизован и связан только с V5; guard и dry-run прошли.
- [x] Чистый foundation схем источников/партий, core-справочников и analytics применён без исторической V4/CX-цепочки; журнал, db lint, health RPC, anon-изоляция и read-only smoke проверены.
- [x] Добавлены закрытый bootstrap первого администратора, admin-only управление ролями/кабинетами и журнал изменений доступа.
- [x] Транзакционно проверить admin/viewer/importer, запрет импорта viewer, разрешённую партию importer и кабинетные ограничения без сохранения fixtures.
- [ ] Перед релизом проверить в браузере отдельных реальных viewer/importer: viewer видит только серверный «Рынок», importer — «Рынок» и ограниченный V5-импорт, admin — полный переходный интерфейс; первый admin уже создан безопасным bootstrap.
- [x] Для пилота «Рынок» реализовать private Storage, сырые строки, серверную валидацию, атомарную публикацию, ошибки строк, идемпотентность, версионную замену и откат.
- [x] Зафиксировать транспортную границу XLSX: разбор в изолированном browser Web Worker, серверная повторная валидация и публикация; небезопасный `xlsx@0.18.5` не использовать в новом пути.
- [x] Ввести минимальные роли viewer/importer/admin и кабинетные ограничения RLS.
- [x] Подключить frontend к серверной роли, закрыть local-first страницы для non-admin и заменить V4-админку на V5-каталог Auth-пользователей с журналируемым назначением доступа.
- [x] Проверить backend-пилот «Рынок» на контрольных строках, периодных формулах, повторной версии, откате и ошибочных данных без изменения методологии.
- [x] Реализовать Web Worker/parser и UI ручного импорта «Рынка», сохраняя исходник до staging; добавить клиентские contract-тесты.
- [x] Вывести bounded историю последних партий «Рынка» со статусом, периодом, счётчиками ошибок/попыток и контролем наличия исходника.
- [x] Добавить on-demand построчную диагностику до 100 ошибок выбранной партии без выдачи raw/staging-таблиц frontend.
- [x] Добавить скачивание сохранённого исходника доступной партии через private Storage без публичных ссылок.
- [x] Добавить on-demand хронологию этапов выбранной партии без выдачи raw-таблицы событий frontend.
- [x] Сформировать контрольный пакет из реального V4-отчёта и сверить V4/V5 на одном периоде. Команда `db:v5:smoke-market-file` не сохраняет исходник в Git и откатывает серверную проверку.
- [ ] Выполнить [ручную приёмку пилота «Рынок»](V5_MARKET_ACCEPTANCE.md): импорт, повтор, ошибочная партия, скачивание исходника, страница и отдельные admin/importer/viewer; до этого UI-путь не считать принятым.
- [x] Подключить только страницу «Рынок» к bounded RPC через V5-only feature flag без неявного fallback на локальные данные.
- [ ] После ручного импорта проверить страницу «Рынок» и downstream «Поисковые запросы»/«Отчётность»; до отдельного переноса они остаются на локальном V4-store.
- [ ] Создать backup-пакет и доказать restore БД, Auth mapping и Storage в отдельный recovery-проект до первой постоянной рабочей загрузки. Процедура и prerequisite-check подготовлены; read-only Storage listing проверен, но дамп блокирует отсутствие Docker/PostgreSQL tools, а тариф платформенных backup ещё не сверён.
- [x] Создать пустую серверную модель товаров, кабинетных SKU/WB ID/алиасов и датированных склеек; проверить переходы и явное «Без склейки» транзакционно.
- [ ] Реализовать серверный импорт справочника и истории склеек, затем сверить его с контрольным экспортом V4; до этого не переключать общие фильтры и товарные страницы.

Порядок последующих страниц предварительный и требует уточнения ежедневной важности владельцем: Конкуренты → География → CX hardening → Точки входа → Поисковые запросы → Воронка/реклама → Рентабельность → Планирование → Главная/товар → Отчётность.

## V4 — поддержка production

- [ ] Проверить `CR заказа` на уровнях категории, склейки и артикула; итог кабинета уже считается корректно.
- [ ] Сверить «Воронку» с Dashboard на одинаковом периоде и фильтрах.
- [ ] Добавить диагностику покрытия после импорта: даты, кабинеты и отсутствующие метрики источника.
- [ ] Провести ручную валидацию CX-словаря и зафиксировать precision/recall основных тем.
- [ ] Добавить мониторинг качества CX и экспорт агрегированной детализации тем.
- [ ] Добавить единый месячный источник планов ДРР и рекламного бюджета.

Эти задачи не переносятся в V5 автоматически и не расширяют текущий backend-этап.

## Замороженный backlog

- [ ] Объединение поисковых запросов WB и продавца — только после серверного индексированного хранения, агрегатов и пагинации для объёма от 250 тысяч строк в месяц.

## Правило обновления

- Новая незавершённая работа получает проверяемый результат и контур V4/V5.
- Завершённая задача удаляется отсюда и при необходимости фиксируется в документе версии.
- Формула, хранение, общий UI-паттерн, документ, навык или агентская конфигурация обновляются вместе с профильным документом и строкой [реестра](INDEX.md).
