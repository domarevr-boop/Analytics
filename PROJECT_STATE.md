# Текущее состояние Analytics MVP

Обновлено: **16 сентября 2026**.

## Активная версия

- Production: **v4.0**, ветка `main`, исходный commit перехода `8dd7db8`. Рабочая V4 сохранена без изменений.
- Активная разработка: **v5.0**, ветка `v5/backend-foundation`, отдельный worktree `C:\Users\vipdo\Documents\test\Analytics-v5`.
- Цель V5 — отдельный Supabase/PostgreSQL-контур, сохранение исходных загрузок, серверный импорт и воспроизводимые версионированные расчёты. Серверные режимы подготовлены для «Географии», «Конкурентов», «Точек входа», «Поисковых запросов» и «Воронки/рекламы»; ручная приёмка ролей перенесена в финальный этап, Auth/RLS остаются обязательными на каждом шаге.
- Отдельный облачный Supabase-проект V5 создан: его URL и publishable key находятся только в игнорируемом `.env.local`, project ref отличается от V4. CLI link указывает только на V5; активная цепочка `20260906000000`–`20260916030000`, включая server import/read «Конкурентов», «Географии», «Точек входа», «Поисковых запросов» и «Воронки/рекламы», а также первый server-import этап «Рентабельности», применена к V5. `20260916028000` идемпотентно задаёт два канонических кабинета для автомаршрутизации; `20260916029000` даёт только полной публикации воронки локальный лимит 120 секунд без расширения общего API-лимита. Remote dry-run, db lint и rollback-smoke проходят. Docker для локального Supabase на машине отсутствует.
- Отдельная проверочная сборка V5 публикуется по адресу `https://domarevr-boop.github.io/Analytics/v5/`; корень `/Analytics/` продолжает отдавать V4. Следующая V5-сборка включает server-only «Рынок», «Конкурентов», «Поисковые запросы», «Точки входа» и «Географию», а также gated импорт «Воронки и рекламы». Кабинетные импорты повторяют правило V4: seller SKU с префиксом 3/4 направляется в «Светпланет», с префиксом 5 — в «Ледситипро»; смешанный файл делится на отдельные серверные партии. Обычный workflow V4 сохраняет подкаталог V5.

## Архитектура

- Клиент: React 19 + TypeScript + Vite.
- Основные отчёты, планы, справочник и вычисляемые витрины работают local-first в браузере; «Отчётность» объединяет собственные локальные данные с агрегированным локальным отчётом «Рынок».
- В worktree V5 страницы «Рынок», «Конкуренты», «Точки входа», «Поисковые запросы» и «География» имеют отдельное opt-in серверное чтение. При серверной ошибке локальные строки V4 не подмешиваются; поиск получает агрегаты, ряды и пагинацию через bounded RPC и связывает денежную оценку с серверным «Рынком».
- Отзывы покупателей — отдельный серверный контур Supabase: импорт, дедупликация, таксономия, тональность, CXI, агрегаты и пагинация.
- Исходные файлы отзывов не хранятся постоянно: после пакетной записи и сверки числа строк временный объект удаляется.
- GitHub Pages публикует статический frontend. Секреты Supabase не должны попадать в клиентскую сборку; доступ регулируется RLS и RPC.

## Подтверждённый аудит для V5

- IndexedDB V4 использует схему 11 и 24 хранилища; импорт распознаёт 12 типов источников. Основная загрузка, валидация, сопоставление и merge выполняются в браузере.
- `CloudRepository` покрывает только часть сущностей и возвращает пустые наборы для истории склеек, географии, точек входа, поиска, рынка и конкурентов; большие наборы всё ещё загружаются во frontend. Это не готовая основа V5.
- CX подтверждает пригодность отдельных паттернов — Auth/RLS, private Storage, партии, дедупликация, версии методологии, RPC и пагинация. Его импорт при этом парсит и нормализует файл на клиенте, пишет несколькими запросами и удаляет исходник после проверки, поэтому целиком как универсальный backend не переносится.
- Серверных расписаний/фоновых заданий и доказанного восстановления Supabase пока нет. Recovery-процедура и безопасная проверка prerequisites подготовлены; CLI умеет читать private Storage V5, где после принятого импорта «Конкурентов» уже есть рабочий исходник, но дамп БД блокирует отсутствие Docker/PostgreSQL tools, а Auth mapping требует отдельной проверки. Frontend V5 читает серверную роль: viewer допускается только к RPC-странице «Рынок», importer — также к ограниченному импорту «Рынка» и включённому импорту «Конкурентов», admin — к полному переходному интерфейсу. Реальные отдельные аккаунты ещё не проверены.
- Историческая цепочка `supabase/migrations/` относится к V4/CX и не подтверждена как чистый bootstrap V5. Из ветки V5 удалены унаследованные CLI-link metadata и дублирующий устаревший `supabase/migration.sql`.
- Историческая цепочка V4/CX сохранена в `supabase/legacy-v4/` и исключена из активного `db push`. Активная цепочка V5 содержит foundation, управление доступом и первый источник `market_dynamics`: версионные дневные факты, сырые строки, серверную валидацию, атомарную публикацию, детерминированную замену версии, откат и агрегаты день/неделя/месяц. Project ref заблокирован защитным скриптом.

## Реализованные рабочие разделы

- Главная: месячные KPI, иерархическая таблица, планы, финансовые показатели, рекламные и воронковые метрики, связанные графики.
- Импорт: распознавание источников, точечная замена метрик, кабинетно-безопасное обновление, покрытие данных.
- Планирование: отдельные сворачиваемые годовые таблицы кабинетов с категориями, точные месячные планы и независимая сценарная модель Plan/Scenario/Δ. Утверждённый план редактирует сумму заказов, процент выкупа, процент к перечислению и рентабельность; сценарий — пять независимых драйверов. Применение сценария создаёт локальную резервную копию для однократного отката. Бренды и товары без категории исключены.
- Аналитика: воронка, точки входа, география заказов, поисковые запросы WB, рынок, конкуренты и «Отчётность». «Рынок» хранит ежедневные суммы и количество заказов, доли и средние чеки; его средний чек используется для денежной оценки поискового спроса. «Отчётность» сравнивает равные периоды, показывает собственные KPI и рынок, а также детализацию собственных показателей по кабинетам. Страница конкурентов использует локальный атомарный импорт четырёх листов Excel, сравнивает срез с нашим ассортиментом, анализирует многосерийную динамику и складские снимки, отдельно отслеживает структуру TOP‑10/20/50.
- Карточка товара: общие KPI, динамика, воронка, источники трафика и география.
- Клиентский опыт: обзор, отзывы, темы, методология и настройки анализа.

## Клиентский опыт

- Темы определяются опубликованным версионируемым словарём: `exact_keyword`, `lemma`, `lemma_phrase`, `context`.
- Тональность определяется отдельно от поиска темы и хранится на уровне совпадения.
- Тональность темы: `Positive / (Positive + Negative) × 100`; при отсутствии оценочных упоминаний возвращается `null`.
- CXI рассчитывается по оценочным упоминаниям и разделён на Overall, Product, Service и Outcome.
- Доступны полный перерасчёт при изменении логики и диапазонный перерасчёт для новых дат.
- Начальный период страницы — последний месяц, в котором есть данные.

## Канонические правила UI

- Поэтапная унификация интерфейса завершена: все продуктовые, CX- и служебные страницы, включая «Разработку», приняты. После миграции удалены только доказанно неиспользуемые legacy-селекторы; массовая очистка `App.css` не выполнялась. Итог программы зафиксирован в `docs/DESIGN_SYSTEM_MIGRATION.md`.
- Главная задаёт эталонный контракт фильтра периода и сущностей.
- KPI выводятся отдельными плитками с единым строением.
- Плотные временные таблицы используют фиксированный левый контекст и фиксированную ширину колонок дат.
- Выбор метрик, select и segmented controls переиспользуют общие визуальные контракты.
- Текущая дизайн-система: `docs/DESIGN_SYSTEM.md`; компоненты: `docs/UI_COMPONENTS.md`.

## Активные ограничения

- Большая часть данных зависит от IndexedDB конкретного браузера и не синхронизируется между устройствами.
- Версия локальной IndexedDB изменяется только монотонно; текущая схема — 11. В ней хранятся история склеек, совокупные планы и глобальная настройка источника плана.
- Глобальный переключатель источника плана действует на все месяцы: при выборе совокупного плана отсутствующий месяц не подменяется прежним поартикульным планом. Сценарная модель не влияет на главную.
- История склеек импортируется локально по ключу `date + product_id`. Пустой код — явная группа «Без склейки», отсутствие записи до первой даты — неизвестный состав; в многодневных расчётах группа разрешается отдельно для каждого дня.
- В V5 создана серверная схема брендов, категорий, товаров, кабинетных алиасов, склеек и датированных состояний состава. Товар, seller SKU, WB ID, алиас и склейка изолированы кабинетом; серверный resolver отличает неизвестное состояние до первой записи от явной группы «Без склейки». Основной путь больше не требует постоянного bootstrap-каталога: миграция `20260911019000` после полной валидации импорта повторяет V4-логику точного/канонического seller SKU, WB SKU и алиасов, создаёт отсутствующий товар, сохраняет batch-lineage и назначает датированное «Без склейки». Bounded RPC выдаёт до 500 доступных пользователю товаров, поддерживает поиск по алиасам и разрешает последнюю известную склейку на выбранную дату; UI управления реестром пока остаётся local-first.
- Контрольный backup V4 от 6 сентября найден вне Git и проверен потоково: 406 210 120 байт; 1 068 строк товаров сводятся к 536 точным identity-компонентам. Локальный игнорируемый Git bootstrap-манифест подготовил 528 товаров, 33 группы и 1 441 датированное состояние; 8 неоднозначных компонент и все 17 связанных строк истории изолированы для разбора, но сохранены рядом с кандидатами. Для них сформированы локальный человекочитаемый отчёт `.v5-local/directory-review.md` и привязанный к SHA-256 backup и отдельному fingerprint очереди пустой шаблон `.v5-local/directory-review-decisions.json`; ни одно решение пока не принято. Принятая часть прошла проверку ссылок и уникальности; в Supabase она ещё не записывалась. Подробности и SHA-256 — в `docs/PRODUCT_REGISTRY.md`.
- В существующий Import UI добавлен admin-only путь JSON-bootstrap с клиентской проверкой, private Storage и атомарной серверной публикацией. Он сохранён как необязательный инструмент исторического переноса и физически выключен переменной `VITE_V5_DIRECTORY_BOOTSTRAP_ENABLED=false`. Типизированный frontend-адаптер bounded чтения и фильтров также готов, но не подключён к странице и закрыт отдельным флагом `VITE_V5_DIRECTORY_BACKEND_ENABLED=false`.
- Рост локальной базы требует диапазонных выборок, кэша агрегатов и виртуализации таблиц.
- План ДРР и рекламного бюджета пока не имеет единого месячного источника.
- ABC и бизнес-роли на странице рентабельности являются периодными относительными классификациями: ABC использует границы накопленной доли 80/95, а роль рассчитывается внутри склейки по классам выручки, положительной ЧП и рекламных расходов.
- Подтверждённая рекламная атрибуция точек входа сейчас не реализована и не назначена будущей версии.
- Массовое объединение поисковых запросов WB и продавца остаётся замороженным backlog из-за объёма данных.
- Аудит `DailyMetrics` для переноса «Воронки/рекламы» подтвердил 174 818 строк воронки по 1 016 товарам и 1 959 рекламных строк по 88 товарам. Исправленный контракт реализован end-to-end: `Заказы, шт → ad_orders_qty`, `Заказы, руб → ad_ordered_amount`, `CPO = ad_spend / ad_orders_qty`; source-aware версии с nullable patch-полями, private import, bounded read API, server-only страница и Import UI прошли unit/contract/build и remote rollback-smoke. Постоянно опубликованы обе рабочие части: `Xway_data (1).xlsx` — 3 568 строк за 01–27.06.2026, 1 732 «Светпланет» и 1 836 «Ледситипро»; сырой WB `Июль_2026 (4).xlsx` — 8 116 строк за 01–18.07.2026, 6 910 «Светпланет» и 1 206 «Ледситипро». Все строки приняты автоматически определёнными кабинетными партиями. Server-only флаг включён в следующую сборку; осталась browser-приёмка объединённой страницы после обновления Pages.
- Полный контрольный файл «Точки входа» прошёл parser, rollback-smoke и постоянный admin-импорт в dev V5: 6 593 строки за 18.07.2026, 5 096 «Светпланет» и 1 497 «Ледситипро», 401 товар. Browser-приёмка server-only страницы и фильтра «Поиск» завершена; local fallback не используется.
- `Лето (2).xlsx` постоянно опубликован в dev V5 без отклонений: 14 659 строк поисковых запросов за 19.07–01.09.2026. Server-only страница принята под admin: общий срез содержит 3 016 ключей, фильтр «Люстры» — 367, графики/Top‑250 загружаются, серверная пагинация переключается на 2/8 без V4-fallback.
- Для «Рентабельности» backup подтвердил 15 756 однодневных строк и параллельный финансовый слой `DailyMetrics`; реальный Excel из 5 437 строк доказал формулу валовой прибыли по пяти расходным компонентам. Миграция `20260916030000` применяет versioned storage, private import, server validation, import-driven товары, generated gross profit/margin и rollback; удалённый smoke прошёл с полным откатом. `actual_profit` V4 фактически означает валовую прибыль, маржа пересчитывается из сумм, а дробное `Количество` не подменяет WB-заказы. Постоянные кабинетные расходы отсутствуют в backup: до доверенного источника настройки net profit остаётся неизвестным, не 0. Bounded read API, server-only UI и постоянная публикация реального Excel ещё не выполнены.
- Доля на странице «Конкуренты» ограничена составом загруженного файла и не должна называться долей всего рынка; наш ассортимент определяется по совпадению WB ID со справочником.
- Для следующего V5-сценария «Конкуренты» проверен контрольный backup: 50 строк воронки, 6 277 запросов, 1 978 остатков и 100 позиций, без повреждённых дат. Всего 512 внешних WB ID; 458 артикулов поискового листа и 4 артикула TOP отсутствуют в воронке, поэтому четыре листа хранятся независимо внутри одной полной версии batch. В поисковом листе обнаружено 143 исходных значения конверсии выше 100%; V5 сохраняет их без подмены. Контрольный `Копия Список ТОП-50.xlsx` канонизирован до 8 405 строк и постоянно опубликован через Import UI без отклонений и ошибок; исходник сохранён в private Storage. Server-only страница включена в staging и принята в браузере на постоянной партии: фильтр `Home One`, сброс, пагинация 1–15/16–30 и вкладка TOP‑50 работают без V4-store. Отдельная importer-проверка перенесена в единый финальный ролевой gate. Контракт зафиксирован в `docs/V5_COMPETITORS_MIGRATION.md`.
- География поддерживает общий фильтр «Все / FBO · Склад WB / FBS · Маркетплейс». FBO и FBS собираются из соответствующих локальных и нелокальных колонок отчёта только внутри расчётов; локальность не является UI-срезом. Версионная серверная таблица, кабинетный RLS, strict-балансы и bounded API применены миграциями `20260910015000`–`20260910018000`; `20260911019000` добавляет автоматическое создание и повторное разрешение товаров из импорта. Безопасный parser/Web Worker и gated Import UI автоматически определяют кабинет по seller SKU и делят смешанный файл на кабинетные партии. Реальный `Геоография заказов.xlsx` постоянно опубликован: 1 059 строк за 18.07.2026, 810 «Светпланет» и 249 «Ледситипро», 2 957 FBO-заказов. Server-only флаг включён в следующую V5-сборку; осталась browser-приёмка страницы после обновления Pages. Финансовая атрибуция ждёт денежных источников и в серверном режиме явно не показывается. Контракт зафиксирован в `docs/V5_GEOGRAPHY_MIGRATION.md`.
- Отчёт «Рынок» не содержит измерений кабинета, категории, бренда или товара; «Отчётность» не применяет к нему фиктивные entity-фильтры и не распределяет рынок по кабинетам.
- Backend-пилот V5 «Рынок» ограничивает исходный файл 10 MiB, один staging-вызов 500 строками, партию 50 000 строками и серверную выборку пятью годами. XLSX/CSV подключены к Import UI через `read-excel-file` в browser Web Worker; сервер сохраняет исходник, повторно валидирует строки и публикует партию атомарно. Перед повторной передачей неопубликованной партии staging и прежние ошибки очищаются серверной транзакцией, исходник сохраняется; опубликованные партии сбросить нельзя. Importer может скачать исходник собственной/разрешённой партии, admin — любой доступной партии; доступ дополнительно ограничен RLS приватного bucket. Ручная приёмка в авторизованном браузере ещё не выполнена.
- Оставшаяся ручная приёмка пилота зафиксирована в `docs/V5_MARKET_ACCEPTANCE.md`: три отдельных аккаунта, успешный/повторный/ошибочный импорт, скачивание исходника и сверка страницы. До её завершения downstream-потребители и новые страницы не переключаются на V5.
- Кабинетные ограничения уже возвращаются в UI-контракте доступа, но пока не используются для клиентской фильтрации: единственная non-admin страница «Рынок» не имеет измерения кабинета. Каждая следующая страница открывается ролям только после переноса её данных и серверного применения кабинетного RLS; local-first страницы non-admin скрыты.
- `npm audit --omit=dev` фиксирует одну high-уязвимость унаследованной production-зависимости `xlsx@0.18.5` без автоматического исправления в npm. Новый UI-путь «Рынка» её не использует; `xlsx` остаётся в legacy V4 и в read-only сверке доверенного контрольного файла.

## Документация и рабочие копии

- Единственный реестр документов, навыков, агентских и инфраструктурных конфигураций — `docs/INDEX.md`; незавершённые задачи хранятся только в `docs/ROADMAP.md`.
- Этапы, зависимости, проверки, release gates и откат V5 зафиксированы в `docs/V5_MIGRATION_PLAN.md`.
- Согласованные карты развития, инфраструктуры и методологии переведены в проверяемые границы V5/V6/V7 и gap-анализ в `docs/V5_TARGET_ARCHITECTURE.md`.
- Для продолжения масштабной UI-миграции после сжатия контекста используется `docs/DESIGN_SYSTEM_MIGRATION.md`; он не заменяет канонические визуальные правила.
- История и отменённые эксперименты находятся в `docs/archive/` и не являются действующими требованиями.
- Живое production-состояние V4 определяется `origin/main`; V5 — только веткой `v5/backend-foundation`. Другие worktree могут содержать старые или незавершённые изменения; перенос из них допускается только после отдельного сравнения.

## Проверки

- Базовая проверка worktree V5 от 16 сентября 2026: 127 профильных infrastructure/contract-тестов проходят; отдельные server-only UI «Конкурентов», «Географии», «Точек входа» и «Поисковых запросов» собираются, а новые/изменённые файлы проходят focused ESLint. Добавлены тесты автоматической кабинетной маршрутизации V4 для префиксов 3/4/5, смешанных файлов и неизвестных префиксов, а также seed канонических кабинетов. Известный полный lint baseline V4 отдельно не исправляется.
- Удалённая проверка V5: guard успешен; migration chain применена по `20260916030000`. Lifecycle/read smoke «Поисковых запросов», «Точек входа», «Конкурентов», «Географии» и «Воронки/рекламы» сохраняют силу; новый lifecycle-smoke «Рентабельности» также прошёл. Все тестовые fixtures и Storage-объекты откатились. Первый Auth-пользователь — admin; межпользовательская изоляция отдельными реальными аккаунтами остаётся финальным release gate.
- Recovery-аудит: `npm run db:v5:backup:check` не выводит секреты и подтверждает готовность CLI для отдельной выгрузки Storage. После принятой партии «Конкурентов» private bucket содержит рабочий исходник, поэтому его отдельная выгрузка теперь обязательна. Docker, `pg_dump`, `pg_restore` и `psql` отсутствуют, поэтому фактический backup/restore не заявляется; процедура находится в `docs/V5_BACKUP_RECOVERY.md`.
- Полный `npm run lint` воспроизводит унаследованный baseline V4: 53 проблемы (27 ошибок, 26 предупреждений). В рамках подготовки V5 они не исправлялись, чтобы не смешивать изоляцию с массовым рефакторингом.
- `npm run build`
- `npm run lint`
- `npm run test:cx`
- `npm run test:reporting`
- `npm run test:planning`
- `npm run test:dashboard`
- Публикация: `npm run deploy`
